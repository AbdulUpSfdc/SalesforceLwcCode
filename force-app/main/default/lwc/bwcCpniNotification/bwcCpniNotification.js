import { LightningElement } from 'lwc';
import cpniImpliedNotification from '@salesforce/label/c.BWC_CpniImpliedNotification';

export default class BwcCpniNotification extends LightningElement {
    labels = {
        cpniImpliedNotification
    }
}
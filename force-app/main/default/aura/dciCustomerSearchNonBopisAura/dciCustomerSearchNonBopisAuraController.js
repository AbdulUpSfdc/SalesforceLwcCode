({
    handleMobile : function(component, event, helper) {
        helper.handleMobile(component, event, helper);
    },
    
    handleDesktop : function(component, event, helper) {
        helper.handleDesktop(component, event, helper);
    }
})